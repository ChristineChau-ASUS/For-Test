name: Target Date Reminder

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  check-target-date:
    runs-on: ubuntu-latest

    steps:
      - name: Check target date rule
        run: |
          echo "Target date reminder workflow is running."
          echo "Rule: Target Date < today and Status != Done"
